namespace KHost.Mobile.UI.Components.Icons;

public sealed partial class ChevronLeftIcon
{
    /// <summary>Width/height as a CSS length. Defaults to <c>em</c> so the icon tracks the surrounding font size.</summary>
    [Parameter] public string Size { get; set; } = "1em";
}
