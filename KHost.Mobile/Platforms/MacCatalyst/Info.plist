<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- The Mac App Store requires you specify if the app uses encryption. -->
    <!-- Please consult https://developer.apple.com/documentation/bundleresources/information_property_list/itsappusesnonexemptencryption -->
    <!-- <key>ITSAppUsesNonExemptEncryption</key> -->
    <!-- Please indicate <true/> or <false/> here. -->

    <!-- Specify the category for your app here. -->
    <!-- Please consult https://developer.apple.com/documentation/bundleresources/information_property_list/lsapplicationcategorytype -->
    <!-- <key>LSApplicationCategoryType</key> -->
    <!-- <string>public.app-category.YOUR-CATEGORY-HERE</string> -->
    <!-- 2 = iPad idiom, 6 = Mac idiom ("Optimize Interface for Mac"). Without 6, Catalyst runs the scaled-iPad
         mode that draws everything at 77%, so the 393 x 852 preview viewport occupies 303 x 657 macOS points and a
         2x screen capture comes out 606 x 1314 — off the 786 x 1704 screenshot grid, and every layout measurement
         is off by the same 1.3x. The Mac idiom maps 1 point to 1 point, which is the whole reason this head
         exists. Nothing in the app branches on DeviceInfo.Idiom, so this only affects scaling. -->
    <key>UIDeviceFamily</key>
    <array>
        <integer>2</integer>
        <integer>6</integer>
    </array>
    <key>UIRequiredDeviceCapabilities</key>
    <array>
        <string>arm64</string>
    </array>
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
    <key>UISupportedInterfaceOrientations~ipad</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
    <key>XSAppIconAssets</key>
    <string>Assets.xcassets/appicon.appiconset</string>
    <!-- Required by MauiLocationProvider (venue auto-select). Apple ABORTS the process — not a catchable
         exception — the moment location authorization is requested without this string, so its absence would
         crash rather than degrade. No NSCameraUsageDescription: the QR scanner is Android/iOS only and resolves
         to UnsupportedQrScanner here, so this head never touches the camera. -->
    <key>NSLocationWhenInUseUsageDescription</key>
    <string>Set your active venue automatically based on where you are.</string>
</dict>
</plist>
